# Music Library System (lab)

This is a small Python project that implements a `Song` class which tracks
global statistics: total songs, unique artists and genres, and counts per
artist/genre.

Run tests with:

```bash
pip install -r requirements.txt
pytest -q
```
